/**
 * Sidebar — left navigation sidebar
 * TODO: Stage 2 — implement with nav links, collapse state, active highlights
 */

export default function Sidebar() {
  return (
    <aside className="w-60 border-r border-neutral-200 bg-white h-full">
      {/* Navigation links — Stage 2 */}
    </aside>
  )
}
