/**
 * Navbar — top navigation bar
 * TODO: Stage 2 — implement with auth, user menu, notifications
 */

export default function Navbar() {
  return (
    <header className="h-14 border-b border-neutral-200 bg-white flex items-center px-6">
      <span className="text-sm font-semibold text-neutral-900">Job Tracker Pro</span>
    </header>
  )
}
